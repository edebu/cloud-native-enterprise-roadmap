# Product Catalog API — Python Dependencies
#
# Pin to minor versions (not patch) to get security fixes automatically
# while avoiding unexpected breaking changes.
# Exact pinning is done in requirements-lock.txt (generated via pip-compile).

# Web framework
fastapi==0.115.*
uvicorn[standard]==0.34.*
starlette>=0.49.1

# Database — async PostgreSQL driver
sqlalchemy[asyncio]==2.0.*
asyncpg==0.30.*

# Data validation
pydantic==2.11.*

# Observability — Prometheus metrics (Phase 5 hook)
prometheus-client==0.21.*

# ---------------------------------------------------------------------------
# Test dependencies
# (also installed in CI — no separate requirements-test.txt needed)
# ---------------------------------------------------------------------------

# Async HTTP test client for FastAPI (replaces TestClient for async routes)
httpx==0.28.*

# Async pytest plugin (asyncio_mode=auto in pyproject.toml)
pytest-asyncio==0.26.*

# Async SQLite driver — replaces asyncpg in tests (no real DB needed in CI)
aiosqlite==0.21.*
