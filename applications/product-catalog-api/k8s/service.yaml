apiVersion: v1
kind: Service
metadata:
  name: product-catalog-api-service
  namespace: cn-er-dev
  labels:
    app.kubernetes.io/name: product-catalog-api
    environment: dev
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app.kubernetes.io/name: product-catalog-api
